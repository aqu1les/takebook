import { StyleSheet } from 'react-native';

export default Styles = StyleSheet.create({
    LoadingContainer: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    }
});
